# Hermes Demos (GitHub Pages)

This repo hosts **static demo websites** generated by Hermes.

- Demos live under `demos/{slug}/`
- Each demo folder contains `index.html` and optional `meta.json`/assets

Published via GitHub Pages (GitHub Actions).

